"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.$broadcast = exports.$deregister = exports.$register = void 0;
// Generated by CoffeeScript 2.5.1
// Stores all the events
var events;
events = {};

var $register = function $register(scope, name, fun) {
  if (events[name] == null) {
    events[name] = {};
  }

  return events[name][scope] = fun;
};

exports.$register = $register;

var $deregister = function $deregister(scope, name) {
  return delete events[name][scope];
};

exports.$deregister = $deregister;

var $broadcast = function $broadcast(name) {
  var fun, key, ref, results;

  if (name !== "" && events[name]) {
    ref = events[name];
    results = [];

    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    for (key in ref) {
      fun = ref[key];

      if (fun) {
        results.push(fun.apply(void 0, args));
      } else {
        results.push(void 0);
      }
    }

    return results;
  }
};

exports.$broadcast = $broadcast;